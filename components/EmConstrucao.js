export default function EmConstrucao({ title }) {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-brand-700 mb-1">{title}</h1>
      <p className="text-sm text-gray-500">
        Essa tela ainda não foi construída — ela chega numa próxima entrega.
      </p>
    </div>
  );
}
